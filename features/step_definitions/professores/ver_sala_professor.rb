Dado("que existam professores com campos {string} e {string}") do |name, room|
  #@professor = Professor.create(name: name)
  pending
end

Então("devo ir pra uma pagina com detalhes sobre o professor {string} com a sala {string}") do |name, room|
  pending
end
